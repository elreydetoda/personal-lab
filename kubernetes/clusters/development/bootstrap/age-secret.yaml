---
apiVersion: v1
kind: Secret
metadata:
  name: sops-age
  namespace: flux-system
stringData:
  # run the following command to string sub the key:
  # yq --arg 'AGE_KEY' "$(grep -A 3 'Flux' ~/.config/sops/age/keys.txt | tail -n 1)"  --yaml-output '.stringData."age.agekey" = $AGE_KEY' kubernetes/clusters/development/bootstrap/age-secret.yaml | kubectl apply -f -
  age.agekey:
